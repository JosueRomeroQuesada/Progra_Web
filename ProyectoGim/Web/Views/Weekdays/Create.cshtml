@using Domain.Weekdays;
@using Web.Models;
@model CreateWeekday

@{
    ViewData["Title"] = "Create";
}

<br />
<section class="contact-section spad">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-lg-8">
                <div class="leave-comment">
                    <div class="section-title text-center">
                        <h2>REGISTRAR DÍA DE LA SEMANA</h2>
                    </div>
                    <form method="post" class="section-title text-center">
                        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                        <div class="mb-3">
                            <span><label asp-for="@Model.IdDiaSemana" class="form-label">No. IdDiaSemana</label></span>
                            <input id="idDiaSemana" asp-for="@Model.IdDiaSemana" type="text" class="form-control" />
                            <span asp-validation-for="@Model.IdDiaSemana" class="text-danger"></span>
                        </div>
                        <div class="mb-3">
                            <span><label asp-for="@Model.Dia" class="form-label">Día</label></span>
                            <input id="dia" asp-for="@Model.Dia" type="text" class="form-control" />
                            <span asp-validation-for="@Model.Dia" class="text-danger"></span>
                        </div>
                        @{
                            var partialModel = SaveCancelViewModel.Create(0, "/weekdays");
                        }
                        <div class="text-center" class="mb-3">
                            <partial name="_SaveCancelButtons" model="partialModel" />
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</section>
